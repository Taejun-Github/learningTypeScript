const superHeors: string[] = [];
const heroPower: Array<number> = [];

superHeors.push("spiderman");
// superHeors.push(1);
heroPower.push(1);

type User = {
    name: string,
    isActive: boolean,
}

const allUsers: Array<User> = [];
const readOnly: ReadonlyArray<number> = [];
const MLModels: Array<Array<number>> = [
    [255, 255, 255],
    [100, 100, 100],
]

allUsers.push({name: 'user1', isActive: true});


export {}
